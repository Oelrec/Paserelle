import 'package:flutter/material.dart';

class ThematiqueControle extends StatelessWidget {
  const ThematiqueControle({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text("Contrôle")),
      body: const Center(child: Text("Contenu de la page Contrôle")),
    );
  }
}
