import 'package:flutter/material.dart';

class ThematiqueTravail extends StatelessWidget {
  const ThematiqueTravail({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text("Travail")),
      body: const Center(child: Text("Contenu de la page Travail")),
    );
  }
}
